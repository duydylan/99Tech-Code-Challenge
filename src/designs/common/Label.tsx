import styled from '@emotion/styled';
import theme from '@/assets/styles/theme';

export const Label = styled.label`
    display: block;
    margin-bottom: 8px;
    font-size: 14px;
    font-family: ${theme.fFamily2};
    text-align: left;
`;
