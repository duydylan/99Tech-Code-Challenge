import { Container } from '@/designs/common/Container';
import styled from '@emotion/styled';

export const ProblemOneWrapper = styled(Container)`
    display: flex;
    flex-direction: column;
    gap: 40px;
    padding: 20px 15px;
`;
export const Title = styled.h1`
    margin: 0;
    padding: 0;
`;
